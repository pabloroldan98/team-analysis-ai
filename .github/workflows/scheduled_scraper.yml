# .github/workflows/scheduled_scraper.yml
name: Scheduled Scraper - All Data

on:
  workflow_dispatch:
  # schedule:
  #   # 1st of each month at 1:00 AM UTC
  #   - cron: '0 1 1 * *'

concurrency:
  group: scheduled-scraper
  cancel-in-progress: false

jobs:
  # ============================================================
  # LEAGUES - Matrix by league and season (runs in parallel)
  # ============================================================
  scrape-leagues:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        league: [laliga, premier, bundesliga, seriea, ligue1]
        season: ['2025-2026', '2024-2025', '2023-2024', '2022-2023', '2021-2022', '2020-2021', '2019-2020', '2018-2019', '2017-2018', '2016-2017']
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Run leagues scraper for ${{ matrix.league }} (${{ matrix.season }})
        run: |
          python scraping_tasks/scrape_leagues.py --leagues ${{ matrix.league }} --season ${{ matrix.season }}
      
      - name: Upload league artifact
        uses: actions/upload-artifact@v4
        with:
          name: leagues-${{ matrix.league }}-${{ matrix.season }}
          path: data/datasets/leagues/leagues_${{ matrix.league }}_${{ matrix.season }}.json
          retention-days: 1
          if-no-files-found: error

  combine-leagues:
    needs: scrape-leagues
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Download all league artifacts
        uses: actions/download-artifact@v4
        with:
          path: data/
          pattern: leagues-*
          merge-multiple: true
      
      - name: Combine into _all_ files
        run: |
          SEASONS=$(ls data/datasets/leagues/leagues_*.json 2>/dev/null | grep -v '_all_' | grep -v '_OLD' | sed -E 's/.*_([0-9]{4}-[0-9]{4})\.json/\1/' | sort -u)
          for season in $SEASONS; do
            python scraping_tasks/combine_data.py --entity leagues --season $season
          done
          for f in data/datasets/leagues/leagues_all_*.json; do
            s=$(echo "$f" | grep -oP '\d{4}-\d{4}')
            echo "$SEASONS" | grep -q "^${s}$" || rm -f "$f"
          done

      - name: Upload all leagues data
        uses: actions/upload-artifact@v4
        with:
          name: leagues-all-data
          path: data/datasets/leagues/leagues_all_*.json
          retention-days: 1

  # ============================================================
  # TEAMS - Matrix by league and season (runs in parallel)
  # ============================================================
  scrape-teams:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        league: [laliga, premier, bundesliga, seriea, ligue1]
        season: ['2025-2026', '2024-2025', '2023-2024', '2022-2023', '2021-2022', '2020-2021', '2019-2020', '2018-2019', '2017-2018', '2016-2017']
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Run teams scraper for ${{ matrix.league }} (${{ matrix.season }})
        run: |
          python scraping_tasks/scrape_teams.py --leagues ${{ matrix.league }} --season ${{ matrix.season }}
      
      - name: Upload league artifact
        uses: actions/upload-artifact@v4
        with:
          name: teams-${{ matrix.league }}-${{ matrix.season }}
          path: data/datasets/teams/teams_${{ matrix.league }}_${{ matrix.season }}.json
          retention-days: 1
          if-no-files-found: error

  combine-teams:
    needs: scrape-teams
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Download all league artifacts
        uses: actions/download-artifact@v4
        with:
          path: data/
          pattern: teams-*
          merge-multiple: true
      
      - name: Combine into _all_ files
        run: |
          SEASONS=$(ls data/datasets/teams/teams_*.json 2>/dev/null | grep -v '_all_' | grep -v '_OLD' | sed -E 's/.*_([0-9]{4}-[0-9]{4})\.json/\1/' | sort -u)
          for season in $SEASONS; do
            python scraping_tasks/combine_data.py --entity teams --season $season
          done
          for f in data/datasets/teams/teams_all_*.json; do
            s=$(echo "$f" | grep -oP '\d{4}-\d{4}')
            echo "$SEASONS" | grep -q "^${s}$" || rm -f "$f"
          done

      - name: Upload all teams data
        uses: actions/upload-artifact@v4
        with:
          name: teams-all-data
          path: data/datasets/teams/teams_all_*.json
          retention-days: 1

  # ============================================================
  # PLAYERS - Matrix by league and season (runs in parallel)
  # ============================================================
  scrape-players:
    runs-on: ubuntu-latest
    timeout-minutes: 300
    strategy:
      fail-fast: false
      matrix:
        league: [laliga, premier, bundesliga, seriea, ligue1]
        season: ['2025-2026', '2024-2025', '2023-2024', '2022-2023', '2021-2022', '2020-2021', '2019-2020', '2018-2019', '2017-2018', '2016-2017']
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Run players scraper for ${{ matrix.league }} (${{ matrix.season }})
        run: |
          python scraping_tasks/scrape_players.py --leagues ${{ matrix.league }} --season ${{ matrix.season }}
      
      - name: Upload league artifact
        uses: actions/upload-artifact@v4
        with:
          name: players-${{ matrix.league }}-${{ matrix.season }}
          path: data/datasets/players/players_${{ matrix.league }}_${{ matrix.season }}.json
          retention-days: 1
          if-no-files-found: error

  combine-players:
    needs: scrape-players
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Download all league artifacts
        uses: actions/download-artifact@v4
        with:
          path: data/
          pattern: players-*
          merge-multiple: true
      
      - name: Combine into _all_ files
        run: |
          SEASONS=$(ls data/datasets/players/players_*.json 2>/dev/null | grep -v '_all_' | grep -v '_OLD' | sed -E 's/.*_([0-9]{4}-[0-9]{4})\.json/\1/' | sort -u)
          for season in $SEASONS; do
            python scraping_tasks/combine_data.py --entity players --season $season
          done
          for f in data/datasets/players/players_all_*.json; do
            s=$(echo "$f" | grep -oP '\d{4}-\d{4}')
            echo "$SEASONS" | grep -q "^${s}$" || rm -f "$f"
          done

      - name: Upload all players data
        uses: actions/upload-artifact@v4
        with:
          name: players-all-data
          path: data/datasets/players/players_all_*.json
          retention-days: 1

  # ============================================================
  # TRANSFERS - Matrix by league and season (runs in parallel)
  # ============================================================
  scrape-transfers:
    runs-on: ubuntu-latest
    timeout-minutes: 300
    strategy:
      fail-fast: false
      matrix:
        league: [laliga, premier, bundesliga, seriea, ligue1]
        season: ['2025-2026', '2024-2025', '2023-2024', '2022-2023', '2021-2022', '2020-2021', '2019-2020', '2018-2019', '2017-2018', '2016-2017']
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Run transfers scraper for ${{ matrix.league }} (${{ matrix.season }})
        run: |
          python scraping_tasks/scrape_transfers.py --leagues ${{ matrix.league }} --season ${{ matrix.season }}
      
      - name: Upload league artifact
        uses: actions/upload-artifact@v4
        with:
          name: transfers-${{ matrix.league }}-${{ matrix.season }}
          path: data/datasets/transfers/transfers_${{ matrix.league }}_${{ matrix.season }}.json
          retention-days: 1
          if-no-files-found: error

  combine-transfers:
    needs: scrape-transfers
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Download all league artifacts
        uses: actions/download-artifact@v4
        with:
          path: data/
          pattern: transfers-*
          merge-multiple: true
      
      - name: Combine into _all_ files
        run: |
          SEASONS=$(ls data/datasets/transfers/transfers_*.json 2>/dev/null | grep -v '_all_' | grep -v '_OLD' | sed -E 's/.*_([0-9]{4}-[0-9]{4})\.json/\1/' | sort -u)
          for season in $SEASONS; do
            python scraping_tasks/combine_data.py --entity transfers --season $season
          done
          for f in data/datasets/transfers/transfers_all_*.json; do
            s=$(echo "$f" | grep -oP '\d{4}-\d{4}')
            echo "$SEASONS" | grep -q "^${s}$" || rm -f "$f"
          done

      - name: Upload all transfers data
        uses: actions/upload-artifact@v4
        with:
          name: transfers-all-data
          path: data/datasets/transfers/transfers_all_*.json
          retention-days: 1

  # ============================================================
  # VALUATIONS - Matrix by league and season (runs in parallel)
  # ============================================================
  scrape-valuations:
    runs-on: ubuntu-latest
    timeout-minutes: 300
    strategy:
      fail-fast: false
      matrix:
        league: [laliga, premier, bundesliga, seriea, ligue1]
        season: ['2025-2026', '2024-2025', '2023-2024', '2022-2023', '2021-2022', '2020-2021', '2019-2020', '2018-2019', '2017-2018', '2016-2017']
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Run valuations scraper for ${{ matrix.league }} (${{ matrix.season }})
        run: |
          python scraping_tasks/scrape_valuations.py --leagues ${{ matrix.league }} --season ${{ matrix.season }}
      
      - name: Upload league artifact
        uses: actions/upload-artifact@v4
        with:
          name: valuations-${{ matrix.league }}-${{ matrix.season }}
          path: data/datasets/valuations/valuations_${{ matrix.league }}_${{ matrix.season }}.json
          retention-days: 1
          if-no-files-found: error

  combine-valuations:
    needs: scrape-valuations
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Download all league artifacts
        uses: actions/download-artifact@v4
        with:
          path: data/
          pattern: valuations-*
          merge-multiple: true
      
      - name: Combine into _all_ files
        run: |
          SEASONS=$(ls data/datasets/valuations/valuations_*.json 2>/dev/null | grep -v '_all_' | grep -v '_OLD' | sed -E 's/.*_([0-9]{4}-[0-9]{4})\.json/\1/' | sort -u)
          for season in $SEASONS; do
            python scraping_tasks/combine_data.py --entity valuations --season $season
          done
          for f in data/datasets/valuations/valuations_all_*.json; do
            s=$(echo "$f" | grep -oP '\d{4}-\d{4}')
            echo "$SEASONS" | grep -q "^${s}$" || rm -f "$f"
          done

      - name: Upload all valuations data
        uses: actions/upload-artifact@v4
        with:
          name: valuations-all-data
          path: data/datasets/valuations/valuations_all_*.json
          retention-days: 1

  # ============================================================
  # COMPETITIONS - Matrix by league and season (runs in parallel)
  # ============================================================
  scrape-competitions:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        league: [laliga, premier, bundesliga, seriea, ligue1]
        season: ['2025-2026', '2024-2025', '2023-2024', '2022-2023', '2021-2022', '2020-2021', '2019-2020', '2018-2019', '2017-2018', '2016-2017']
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Run competitions scraper for ${{ matrix.league }} (${{ matrix.season }})
        run: |
          python scraping_tasks/scrape_competitions.py --leagues ${{ matrix.league }} --season ${{ matrix.season }}
      
      - name: Upload league artifact
        uses: actions/upload-artifact@v4
        with:
          name: competitions-${{ matrix.league }}-${{ matrix.season }}
          path: data/datasets/competitions/competitions_${{ matrix.league }}_${{ matrix.season }}.json
          retention-days: 1
          if-no-files-found: error

  combine-competitions:
    needs: scrape-competitions
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Download all league artifacts
        uses: actions/download-artifact@v4
        with:
          path: data/
          pattern: competitions-*
          merge-multiple: true
      
      - name: Combine into _all_ files
        run: |
          SEASONS=$(ls data/datasets/competitions/competitions_*.json 2>/dev/null | grep -v '_all_' | grep -v '_OLD' | sed -E 's/.*_([0-9]{4}-[0-9]{4})\.json/\1/' | sort -u)
          for season in $SEASONS; do
            python scraping_tasks/combine_data.py --entity competitions --season $season
          done
          for f in data/datasets/competitions/competitions_all_*.json; do
            s=$(echo "$f" | grep -oP '\d{4}-\d{4}')
            echo "$SEASONS" | grep -q "^${s}$" || rm -f "$f"
          done

      - name: Upload all competitions data
        uses: actions/upload-artifact@v4
        with:
          name: competitions-all-data
          path: data/datasets/competitions/competitions_all_*.json
          retention-days: 1

  # ============================================================
  # FINAL COMMIT - All data (waits for all combines)
  # ============================================================
  commit-all:
    needs: [combine-leagues, combine-teams, combine-players, combine-transfers, combine-valuations, combine-competitions]
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false
      
      - name: Download leagues data
        uses: actions/download-artifact@v4
        with:
          name: leagues-all-data
          path: data/
      
      - name: Download teams data
        uses: actions/download-artifact@v4
        with:
          name: teams-all-data
          path: data/
      
      - name: Download players data
        uses: actions/download-artifact@v4
        with:
          name: players-all-data
          path: data/
      
      - name: Download transfers data
        uses: actions/download-artifact@v4
        with:
          name: transfers-all-data
          path: data/
      
      - name: Download valuations data
        uses: actions/download-artifact@v4
        with:
          name: valuations-all-data
          path: data/
      
      - name: Download competitions data
        uses: actions/download-artifact@v4
        with:
          name: competitions-all-data
          path: data/
      
      - name: List downloaded files
        run: |
          echo "Downloaded files:"
          ls -la data/
      
      - name: Configure git
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
      
      - name: Update auto-update trigger in streamlit_app.py
        run: |
          TRIGGER_LINE="# Auto-update trigger: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          if grep -q "^# Auto-update trigger:" streamlit_app.py; then
            sed -i "s/^# Auto-update trigger:.*/$TRIGGER_LINE/" streamlit_app.py
          else
            echo "$TRIGGER_LINE" >> streamlit_app.py
          fi

      - name: Commit changes
        run: |
          git add data/ streamlit_app.py
          git diff --staged --quiet || git commit -m "Monthly data update [$(date -u '+%Y-%m-%d')]"
      
      - name: Push changes
        uses: ad-m/github-push-action@v0.6.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main
