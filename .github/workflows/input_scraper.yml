# .github/workflows/input_scraper.yml
name: Input Scraper - Custom Leagues & Seasons

on:
  workflow_dispatch:
    inputs:
      leagues:
        description: 'Leagues to scrape (JSON array, e.g., ["laliga", "premier", "bundesliga", "seriea", "ligue1"] or ["laliga"])'
        required: true
        default: '["laliga"]'
        type: string
      seasons:
        description: 'Seasons to scrape (JSON array, e.g., ["2025-2026", "2024-2025"] or ["2025-2026"])'
        required: true
        default: '["2025-2026"]'
        type: string
      scrape_leagues:
        description: 'Scrape Leagues'
        required: false
        default: true
        type: boolean
      scrape_teams:
        description: 'Scrape Teams'
        required: false
        default: true
        type: boolean
      scrape_players:
        description: 'Scrape Players'
        required: false
        default: true
        type: boolean
      scrape_transfers:
        description: 'Scrape Transfers'
        required: false
        default: true
        type: boolean
      scrape_valuations:
        description: 'Scrape Valuations'
        required: false
        default: true
        type: boolean
      scrape_competitions:
        description: 'Scrape Competitions'
        required: false
        default: true
        type: boolean
      scrape_injuries:
        description: 'Scrape Injuries'
        required: false
        default: true
        type: boolean

concurrency:
  group: input-scraper
  cancel-in-progress: false

jobs:
  # ============================================================
  # LEAGUES
  # ============================================================
  scrape-leagues:
    if: ${{ inputs.scrape_leagues }}
    runs-on: ubuntu-latest
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        league: ${{ fromJSON(inputs.leagues) }}
        season: ${{ fromJSON(inputs.seasons) }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Run leagues scraper for ${{ matrix.league }} (${{ matrix.season }})
        run: |
          python scraping_tasks/scrape_leagues.py --leagues ${{ matrix.league }} --season ${{ matrix.season }}
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: leagues-${{ matrix.league }}-${{ matrix.season }}
          path: data/datasets/leagues/leagues_${{ matrix.league }}_${{ matrix.season }}.json
          retention-days: 1
          if-no-files-found: error

  combine-leagues:
    if: ${{ inputs.scrape_leagues }}
    needs: scrape-leagues
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: data/
          pattern: leagues-*
          merge-multiple: true
      
      - name: Combine into _all_ files
        run: |
          SEASONS="${{ join(fromJSON(inputs.seasons), ' ') }}"
          for season in $SEASONS; do
            python scraping_tasks/combine_data.py --entity leagues --season $season
          done
          for f in data/datasets/leagues/leagues_all_*.json; do
            s=$(echo "$f" | grep -oP '\d{4}-\d{4}')
            echo "$SEASONS" | tr ' ' '\n' | grep -q "^${s}$" || rm -f "$f"
          done
      
      - name: Upload all data
        uses: actions/upload-artifact@v4
        with:
          name: leagues-all-data
          path: data/datasets/leagues/leagues_all_*.json
          retention-days: 1

  # ============================================================
  # TEAMS
  # ============================================================
  scrape-teams:
    if: ${{ inputs.scrape_teams }}
    runs-on: ubuntu-latest
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        league: ${{ fromJSON(inputs.leagues) }}
        season: ${{ fromJSON(inputs.seasons) }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Run teams scraper for ${{ matrix.league }} (${{ matrix.season }})
        run: |
          python scraping_tasks/scrape_teams.py --leagues ${{ matrix.league }} --season ${{ matrix.season }}
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: teams-${{ matrix.league }}-${{ matrix.season }}
          path: data/datasets/teams/teams_${{ matrix.league }}_${{ matrix.season }}.json
          retention-days: 1
          if-no-files-found: error

  combine-teams:
    if: ${{ inputs.scrape_teams }}
    needs: scrape-teams
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: data/
          pattern: teams-*
          merge-multiple: true
      
      - name: Combine into _all_ files
        run: |
          SEASONS="${{ join(fromJSON(inputs.seasons), ' ') }}"
          for season in $SEASONS; do
            python scraping_tasks/combine_data.py --entity teams --season $season
          done
          for f in data/datasets/teams/teams_all_*.json; do
            s=$(echo "$f" | grep -oP '\d{4}-\d{4}')
            echo "$SEASONS" | tr ' ' '\n' | grep -q "^${s}$" || rm -f "$f"
          done

      - name: Upload all data
        uses: actions/upload-artifact@v4
        with:
          name: teams-all-data
          path: data/datasets/teams/teams_all_*.json
          retention-days: 1

  # ============================================================
  # PLAYERS
  # ============================================================
  scrape-players:
    if: ${{ inputs.scrape_players }}
    runs-on: ubuntu-latest
    timeout-minutes: 300
    strategy:
      fail-fast: false
      matrix:
        league: ${{ fromJSON(inputs.leagues) }}
        season: ${{ fromJSON(inputs.seasons) }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Run players scraper for ${{ matrix.league }} (${{ matrix.season }})
        run: |
          python scraping_tasks/scrape_players.py --leagues ${{ matrix.league }} --season ${{ matrix.season }}
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: players-${{ matrix.league }}-${{ matrix.season }}
          path: data/datasets/players/players_${{ matrix.league }}_${{ matrix.season }}.json
          retention-days: 1
          if-no-files-found: error

  combine-players:
    if: ${{ inputs.scrape_players }}
    needs: scrape-players
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: data/
          pattern: players-*
          merge-multiple: true
      
      - name: Combine into _all_ files
        run: |
          SEASONS="${{ join(fromJSON(inputs.seasons), ' ') }}"
          for season in $SEASONS; do
            python scraping_tasks/combine_data.py --entity players --season $season
          done
          for f in data/datasets/players/players_all_*.json; do
            s=$(echo "$f" | grep -oP '\d{4}-\d{4}')
            echo "$SEASONS" | tr ' ' '\n' | grep -q "^${s}$" || rm -f "$f"
          done

      - name: Upload all data
        uses: actions/upload-artifact@v4
        with:
          name: players-all-data
          path: data/datasets/players/players_all_*.json
          retention-days: 1

  # ============================================================
  # TRANSFERS
  # ============================================================
  scrape-transfers:
    if: ${{ inputs.scrape_transfers }}
    runs-on: ubuntu-latest
    timeout-minutes: 300
    strategy:
      fail-fast: false
      matrix:
        league: ${{ fromJSON(inputs.leagues) }}
        season: ${{ fromJSON(inputs.seasons) }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Run transfers scraper for ${{ matrix.league }} (${{ matrix.season }})
        run: |
          python scraping_tasks/scrape_transfers.py --leagues ${{ matrix.league }} --season ${{ matrix.season }}
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: transfers-${{ matrix.league }}-${{ matrix.season }}
          path: data/datasets/transfers/transfers_${{ matrix.league }}_${{ matrix.season }}.json
          retention-days: 1
          if-no-files-found: error

  combine-transfers:
    if: ${{ inputs.scrape_transfers }}
    needs: scrape-transfers
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: data/
          pattern: transfers-*
          merge-multiple: true
      
      - name: Combine into _all_ files
        run: |
          SEASONS="${{ join(fromJSON(inputs.seasons), ' ') }}"
          for season in $SEASONS; do
            python scraping_tasks/combine_data.py --entity transfers --season $season
          done
          for f in data/datasets/transfers/transfers_all_*.json; do
            s=$(echo "$f" | grep -oP '\d{4}-\d{4}')
            echo "$SEASONS" | tr ' ' '\n' | grep -q "^${s}$" || rm -f "$f"
          done

      - name: Upload all data
        uses: actions/upload-artifact@v4
        with:
          name: transfers-all-data
          path: data/datasets/transfers/transfers_all_*.json
          retention-days: 1

  # ============================================================
  # VALUATIONS
  # ============================================================
  scrape-valuations:
    if: ${{ inputs.scrape_valuations }}
    runs-on: ubuntu-latest
    timeout-minutes: 300
    strategy:
      fail-fast: false
      matrix:
        league: ${{ fromJSON(inputs.leagues) }}
        season: ${{ fromJSON(inputs.seasons) }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Run valuations scraper for ${{ matrix.league }} (${{ matrix.season }})
        run: |
          python scraping_tasks/scrape_valuations.py --leagues ${{ matrix.league }} --season ${{ matrix.season }}
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: valuations-${{ matrix.league }}-${{ matrix.season }}
          path: data/datasets/valuations/valuations_${{ matrix.league }}_${{ matrix.season }}.json
          retention-days: 1
          if-no-files-found: error

  combine-valuations:
    if: ${{ inputs.scrape_valuations }}
    needs: scrape-valuations
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: data/
          pattern: valuations-*
          merge-multiple: true
      
      - name: Combine into _all_ files
        run: |
          SEASONS="${{ join(fromJSON(inputs.seasons), ' ') }}"
          for season in $SEASONS; do
            python scraping_tasks/combine_data.py --entity valuations --season $season
          done
          for f in data/datasets/valuations/valuations_all_*.json; do
            s=$(echo "$f" | grep -oP '\d{4}-\d{4}')
            echo "$SEASONS" | tr ' ' '\n' | grep -q "^${s}$" || rm -f "$f"
          done

      - name: Upload all data
        uses: actions/upload-artifact@v4
        with:
          name: valuations-all-data
          path: data/datasets/valuations/valuations_all_*.json
          retention-days: 1

  # ============================================================
  # COMPETITIONS
  # ============================================================
  scrape-competitions:
    if: ${{ inputs.scrape_competitions }}
    runs-on: ubuntu-latest
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        league: ${{ fromJSON(inputs.leagues) }}
        season: ${{ fromJSON(inputs.seasons) }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Run competitions scraper for ${{ matrix.league }} (${{ matrix.season }})
        run: |
          python scraping_tasks/scrape_competitions.py --leagues ${{ matrix.league }} --season ${{ matrix.season }}
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: competitions-${{ matrix.league }}-${{ matrix.season }}
          path: data/datasets/competitions/competitions_${{ matrix.league }}_${{ matrix.season }}.json
          retention-days: 1
          if-no-files-found: error

  combine-competitions:
    if: ${{ inputs.scrape_competitions }}
    needs: scrape-competitions
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: data/
          pattern: competitions-*
          merge-multiple: true
      
      - name: Combine into _all_ files
        run: |
          SEASONS="${{ join(fromJSON(inputs.seasons), ' ') }}"
          for season in $SEASONS; do
            python scraping_tasks/combine_data.py --entity competitions --season $season
          done
          for f in data/datasets/competitions/competitions_all_*.json; do
            s=$(echo "$f" | grep -oP '\d{4}-\d{4}')
            echo "$SEASONS" | tr ' ' '\n' | grep -q "^${s}$" || rm -f "$f"
          done

      - name: Upload all data
        uses: actions/upload-artifact@v4
        with:
          name: competitions-all-data
          path: data/datasets/competitions/competitions_all_*.json
          retention-days: 1

  # ============================================================
  # INJURIES
  # ============================================================
  scrape-injuries:
    if: ${{ inputs.scrape_injuries }}
    runs-on: ubuntu-latest
    timeout-minutes: 300
    strategy:
      fail-fast: false
      matrix:
        league: ${{ fromJSON(inputs.leagues) }}
        season: ${{ fromJSON(inputs.seasons) }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Run injuries scraper for ${{ matrix.league }} (${{ matrix.season }})
        run: |
          python scraping_tasks/scrape_injuries.py --leagues ${{ matrix.league }} --season ${{ matrix.season }}
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: injuries-${{ matrix.league }}-${{ matrix.season }}
          path: data/datasets/injuries/injuries_${{ matrix.league }}_${{ matrix.season }}.json
          retention-days: 1
          if-no-files-found: error

  combine-injuries:
    if: ${{ inputs.scrape_injuries }}
    needs: scrape-injuries
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Pre-load TLS client (suppress download progress)
        run: python -c "import tls_requests; tls_requests.get('https://httpbin.org/get', timeout=10)" > /dev/null 2>&1 || true
      
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: data/
          pattern: injuries-*
          merge-multiple: true
      
      - name: Combine into _all_ files
        run: |
          SEASONS="${{ join(fromJSON(inputs.seasons), ' ') }}"
          for season in $SEASONS; do
            python scraping_tasks/combine_data.py --entity injuries --season $season
          done
          for f in data/datasets/injuries/injuries_all_*.json; do
            s=$(echo "$f" | grep -oP '\d{4}-\d{4}')
            echo "$SEASONS" | tr ' ' '\n' | grep -q "^${s}$" || rm -f "$f"
          done

      - name: Upload all data
        uses: actions/upload-artifact@v4
        with:
          name: injuries-all-data
          path: data/datasets/injuries/injuries_all_*.json
          retention-days: 1

  # ============================================================
  # FINAL COMMIT
  # ============================================================
  commit-all:
    needs: [combine-leagues, combine-teams, combine-players, combine-transfers, combine-valuations, combine-competitions, combine-injuries]
    if: |
      always() &&
      (needs.combine-leagues.result == 'success' || needs.combine-leagues.result == 'skipped') &&
      (needs.combine-teams.result == 'success' || needs.combine-teams.result == 'skipped') &&
      (needs.combine-players.result == 'success' || needs.combine-players.result == 'skipped') &&
      (needs.combine-transfers.result == 'success' || needs.combine-transfers.result == 'skipped') &&
      (needs.combine-valuations.result == 'success' || needs.combine-valuations.result == 'skipped') &&
      (needs.combine-competitions.result == 'success' || needs.combine-competitions.result == 'skipped') &&
      (needs.combine-injuries.result == 'success' || needs.combine-injuries.result == 'skipped')
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false
      
      - name: Download leagues data
        if: ${{ inputs.scrape_leagues }}
        uses: actions/download-artifact@v4
        with:
          name: leagues-all-data
          path: data/
        continue-on-error: true
      
      - name: Download teams data
        if: ${{ inputs.scrape_teams }}
        uses: actions/download-artifact@v4
        with:
          name: teams-all-data
          path: data/
        continue-on-error: true
      
      - name: Download players data
        if: ${{ inputs.scrape_players }}
        uses: actions/download-artifact@v4
        with:
          name: players-all-data
          path: data/
        continue-on-error: true
      
      - name: Download transfers data
        if: ${{ inputs.scrape_transfers }}
        uses: actions/download-artifact@v4
        with:
          name: transfers-all-data
          path: data/
        continue-on-error: true
      
      - name: Download valuations data
        if: ${{ inputs.scrape_valuations }}
        uses: actions/download-artifact@v4
        with:
          name: valuations-all-data
          path: data/
        continue-on-error: true
      
      - name: Download competitions data
        if: ${{ inputs.scrape_competitions }}
        uses: actions/download-artifact@v4
        with:
          name: competitions-all-data
          path: data/
        continue-on-error: true
      
      - name: Download injuries data
        if: ${{ inputs.scrape_injuries }}
        uses: actions/download-artifact@v4
        with:
          name: injuries-all-data
          path: data/
        continue-on-error: true
      
      - name: List downloaded files
        run: |
          echo "Downloaded files:"
          ls -la data/ || echo "No data directory"
      
      - name: Configure git
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
      
      - name: Update auto-update trigger in streamlit_app.py
        run: |
          TRIGGER_LINE="# Auto-update trigger: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          if grep -q "^# Auto-update trigger:" streamlit_app.py; then
            sed -i "s/^# Auto-update trigger:.*/$TRIGGER_LINE/" streamlit_app.py
          else
            echo "$TRIGGER_LINE" >> streamlit_app.py
          fi

      - name: Commit changes
        run: |
          git add data/ streamlit_app.py
          git diff --staged --quiet || git commit -m "Custom scraper update [$(date -u '+%Y-%m-%d')]"
      
      - name: Push changes
        uses: ad-m/github-push-action@v0.6.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main
