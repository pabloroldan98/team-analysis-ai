# .github/workflows/scrape_players.yml
name: Scrape Players

on:
  workflow_dispatch:  # Manual trigger (no inputs needed)
  schedule:
    # Weekly on Sunday at 3:00 AM UTC
    - cron: '0 3 * * 0'

jobs:
  scrape-players:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Run players scraper
        run: |
          python scraping_tasks/scrape_players.py
      
      - name: Commit and push data
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/
          git diff --staged --quiet || git commit -m "Update players data [$(date -u '+%Y-%m-%d')]"
          git push
